# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Web Server Configuration
WEB_PORT=3000
WEB_USERNAME=admin
WEB_PASSWORD=admin

# Database Configuration
DB_PATH=./data/database.sqlite
DB_LOGGING=false

# Google Calendar Configuration
# Upload your service account JSON file to: data/calendar-credentials.json
# Then configure calendars via the web UI (Settings → Google Calendar)
GOOGLE_CALENDAR_CREDENTIALS=./data/calendar-credentials.json

# Twitch Configuration (Optional - for stream notifications)
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret

# Timezone (Optional)
TZ=UTC
