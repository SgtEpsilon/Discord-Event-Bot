# ==========================================
# DISCORD BOT CONFIGURATION (REQUIRED)
# ==========================================

# Discord Bot Token (REQUIRED)
# Get from: https://discord.com/developers/applications → Your App → Bot → Token
DISCORD_TOKEN=your_discord_bot_token_here

# ==========================================
# GOOGLE CALENDAR INTEGRATION (OPTIONAL)
# ==========================================

# Service Account JSON Credentials (as single-line string)
# Get from: https://console.cloud.google.com/iam-admin/serviceaccounts
# Steps:
#   1. Create service account
#   2. Generate JSON key
#   3. Share calendar with service account email (your-service-account@your-project.iam.gserviceaccount.com)
#   4. Convert JSON to single line (remove newlines, escape quotes)
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"your-service-account@your-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com"}

# Calendar IDs (Optional - comma separated)
# Format options:
#   Single calendar: "primary"
#   Multiple named: "Work:calendar_id_1,Gaming:calendar_id_2"
#   Multiple unnamed: "calendar_id_1,calendar_id_2"
CALENDAR_IDS=primary

# ==========================================
# STREAMING INTEGRATION (OPTIONAL)
# ==========================================

# Twitch API Credentials (for live stream monitoring)
# Get from: https://dev.twitch.tv/console/apps → Register your application
TWITCH_CLIENT_ID=your_twitch_client_id_here
TWITCH_CLIENT_SECRET=your_twitch_client_secret_here

# YouTube monitoring uses RSS feeds (no API key required)

# ==========================================
# WEB INTERFACE CONFIGURATION (REQUIRED FOR WEB UI)
# ==========================================

# Web API Key (REQUIRED for web authentication)
# Generate secure key with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# OR use: https://generate-secret.vercel.app/32
WEB_API_KEY=your_64_character_hex_api_key_here

# Web Server Port (default: 3000)
WEB_PORT=3031

# Bot PM2 Process Name (REQUIRED for bot restart functionality)
# Only needed if managing bot with PM2 process manager
# Default: discord-event-bot
BOT_PM2_NAME=discord-event-bot

# ==========================================
# CRITICAL SETUP NOTES
# ==========================================

# ✅ REQUIRED DIRECTORIES (create before starting):
#    mkdir -p data
#    touch data/bot-status.json data/guilds.json data/streaming-config.json

# ✅ BOT STARTUP (REQUIRED for web UI to function):
#    Option 1 (PM2 - enables restart button):
#      npm install -g pm2
#      pm2 start src/bot.js --name discord-event-bot
#      pm2 save
#
#    Option 2 (direct - restart button will show instructions):
#      node src/bot.js

# ✅ WEB SERVER STARTUP:
#    node web-server.js

# ✅ PATH CONSISTENCY (critical for streaming config):
#    Both bot and web server MUST use identical path:
#      ./data/streaming-config.json
#    DO NOT use streaming.json (will cause 404 errors)

# ✅ FIRST TIME SETUP:
#    1. Start Discord bot FIRST (generates guilds.json and bot-status.json)
#    2. Then start web server
#    3. Access http://localhost:3000 and enter your WEB_API_KEY